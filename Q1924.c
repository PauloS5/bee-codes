#include <stdio.h>
#include <math.h>

int main() {
	int n, i;
	
	scanf("%d", &n);
	int valores[n];

	for (i = 0; i < n; i++) {
		scanf("%s", &valores[i]);
	}

	printf("Ciencia da Computacao\n");
	
	return 0;
}